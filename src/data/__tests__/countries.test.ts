

describe("country data", () => {
  describe("phoneRegexp", () => {
    it.todo("should validate good and bad phone numbers correctly, with and without stuff from the formatted version like spaces, dashes, etc.")
  })
})